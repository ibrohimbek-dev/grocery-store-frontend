import React from 'react'
import { CardActionsProps } from "../../../lib/types/common";

const DairyProducts = ({ onAdd, cartItems, onDeleteAll }: CardActionsProps) => {
	return <div>DairyProducts</div>;
};

export default DairyProducts